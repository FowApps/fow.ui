import styled from 'styled-components';

export const StyledOverline = styled.p`
    margin: 0;
    font-weight: 600;
    font-size: 1rem;
    overflow: hidden;
    text-overflow: ellipsis;
    line-height: 1.8rem;
    letter-spacing: 1.1;
`;
